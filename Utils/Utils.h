#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>
#include <regex>
using namespace std;

vector<string> split(string stringToSplit, string delimeter);
string filterNumbers(string input);

#endif // UTILS_H
